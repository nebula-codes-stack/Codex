{
  "name": "codex-web-terminal",
  "version": "0.1.0",
  "private": true,
  "description": "A beautiful browser terminal powered by xterm.js and a Node.js PTY backend.",
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "start": "NODE_ENV=production node server/index.js",
    "check": "node --check server/index.js && node --check server/vendor.js && node --check public/main.js && node --check public/settings.js && node --check public/debug.js && bash -n start.sh start.command",
    "postinstall": "node server/vendor.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "express": "^4.19.2",
    "node-pty": "^1.0.0",
    "ws": "^8.17.1"
  }
}
